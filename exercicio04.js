// Desenvolva um programa que crie um array com n frutas e imprima cada uma utilizando um loop.

let frutas = ['Maçã', 'Banana', 'Abacaxi', 'Abacate', 'Mamão'];

for (let i in frutas) {
    console.log(frutas[i]);
}